{% extends 'AppBundle::layout.html.twig' %}

{% block main %}
    <main class="blog edit-post">

        <div class="container">

            <div class="row margin-top">

                <h2 class="title">Edit Post</h2>

                <div class="col-xs-12 col-md-6 col-md-offset-3">

                    {{ form_start(edit_form) }}
                    {{ form_errors(edit_form) }}

                    <div class="form-group">
                        {{ form_errors(edit_form.title) }}
                        {{ form_widget(edit_form.title) }}
                    </div>

                    <div class="form-group">
                        {{ form_errors(edit_form.description) }}
                        {{ form_widget(edit_form.description) }}
                    </div>

                    <div class="form-group">
                        {{ form_errors(edit_form.content) }}
                        {{ form_widget(edit_form.content) }}
                    </div>

                    <div class="form-group">
                        {{ form_errors(edit_form.thumb) }}
                        {{ form_widget(edit_form.thumb) }}
                    </div>

                    <div class="form-group">
                        {{ form_errors(edit_form.thumb) }}
                        {{ form_widget(edit_form.thumb) }}
                    </div>

                    <div class="form-group">
                        {{ form_label(edit_form.author) }}
                        {{ form_widget(edit_form.author) }}
                        {{ form_row(edit_form.author) }}
                    </div>

                    <button type="submit" class="btn btn-default">Submit</button>
                    {{ form_end(edit_form) }}

                </div>


            </div>

        </div>

    </main>

{% endblock main %}
